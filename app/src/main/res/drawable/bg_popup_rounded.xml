<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="90"
        android:centerColor="@color/bg_home_gradient_center_no_transparent"
        android:endColor="@color/bg_home_gradient_end"
        android:startColor="@color/bg_home_gradient_start" />
    <corners android:radius="16dp" />
</shape>
